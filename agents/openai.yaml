interface:
  display_name: "Product UGC Montage"
  short_description: "从商品证据反推 10 秒 Omni 素材，批量混剪不同开头的广告"
  default_prompt: "Use $product-ugc-montage to design 20 distinct creative briefs, derive proof-duration demand, calibrate an authorized 10s Omni reference pilot, allocate existing footage and targeted supplements, then create independent complete narration and validate measured-runtime edits before rendering and QA."

policy:
  allow_implicit_invocation: true
