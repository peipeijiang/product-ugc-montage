interface:
  display_name: "Product UGC Montage"
  short_description: "从商品证据反推 10 秒 Omni 素材，批量混剪不同开头的广告"
  default_prompt: "Use $product-ugc-montage to plan 20 distinct-opening ads by default, budget evidence-backed 10s Omni all-purpose-reference sources, create a separate complete GEM/Doubao narration for each video, selectively share soft BGM, then render product annotations and run per-video and batch QA."

policy:
  allow_implicit_invocation: true
